<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">

<!--
    This XML file contains the values for the username and password fields required to connect
    to the MySQL database. The default username and password are "root" and "root", respectively.
    To change the username and password from "root", simply change the values in both the 'entry' tags.
    -->

<properties>
<comment>Credentials for the database.</comment>
    <entry key="username">root</entry>
    <entry key="password">root</entry>
</properties>
